import React, {FlatList, SafeAreaView, View, Text} from 'react-native';

const Dashboard = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Dashboard</Text>
      </View>
      <FlatList />
    </SafeAreaView>
  );
};

export default Dashboard;
